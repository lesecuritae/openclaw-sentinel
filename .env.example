SENTINEL_PORT=8080
# Generate independent credentials with: openssl rand -hex 32
# Empty credentials grant no access. Bootstrap works only before any user exists.
SENTINEL_API_KEY=
SENTINEL_ADMIN_KEY=
SENTINEL_ANALYST_KEY=
SENTINEL_VIEWER_KEY=
SETUP_BOOTSTRAP_TOKEN=
COLLECTOR_CREDENTIALS={}
LLM_ALLOWED_PROVIDERS=["disabled","local"]
LLM_DATA_CLASSIFICATION=internal
DATABASE_PATH=/data/sentinel.db
RULES_PATH=/app/config/rules.yaml
POLICY_PATH=/app/config/policy.yaml
INTELLIGENCE_PATH=/app/config/intelligence.yaml
HAPROXY_COLLECTOR_ENABLED=false
HAPROXY_SOCKET=/run/haproxy/admin.sock
HAPROXY_BLOCKLIST_PATH=/etc/haproxy/sentinel-blocklist.lst
COLLECTOR_INTERVAL_SECONDS=5
HAPROXY_REQUEST_COLLECTOR_ENABLED=false
HAPROXY_REQUEST_BIND=127.0.0.1
HAPROXY_REQUEST_HOST=0.0.0.0
HAPROXY_REQUEST_PORT=1514
ACTIONS_ENABLED=false
LLM_PROVIDER=disabled
MODEL=
OPENROUTER_API_KEY=
ABUSECH_AUTH_KEY=
LOCAL_LLM_URL=http://host.docker.internal:11434
ANUBIS_URL=
DOCKER_COLLECTOR_ENABLED=false
# Point only to a restricted Docker socket proxy that allows GET /events.
DOCKER_API_URL=
AUTH_COLLECTOR_ENABLED=false
SERVICE_LOG_COLLECTOR_ENABLED=false
AUTH_LOG_PATHS=/var/log/auth.log
SERVICE_LOG_PATH=
# Optional comma-separated exact browser origins. Empty means same-origin only.
CORS_ORIGIN_ALLOWLIST=
WEB_2FA_ENABLED=false
# Supply exactly one base32 secret through the runtime environment or a mounted secret file.
WEB_2FA_SECRET=
WEB_2FA_SECRET_FILE=
WEB_SESSION_TTL_SECONDS=900
RESPONSE_DRY_RUN=true
LLM_TIMEOUT_SECONDS=30
